package org.syriacplatform.packageformat.models

/**
 * يحدد الفئة الوظيفية لحزمة التطبيق.
 *
 * أسماء enum داخلية ولا تمثل بالضرورة النص المخزن في JSON.
 */
enum class PackageProfile {
    OCCASION,
    SHHIMA,
    FULL_LIBRARY
}